**SYSTEM DIRECTIVE:** You are connected to my local filesystem via MCP. You have full read/write access to the `htdocs` directory. Do not claim you lack access.

**Location Instructions:** Inside `htdocs`, look for the folder named `schoolerp/`. All your work will take place inside this folder.

**Task:** Create new Page for the tab in sidebar - Leads/Inquiry
: **[Leads/Inquiry]**

**Target Path:** `schoolerp/modules/school/`
New Folder will be created Here:
Create sql file also so i can pick that and run in my xampp.

- Scan the attached image and identify all UI elements, tables, forms, buttons, filters, actions, fields, modals, tabs, and features.
- Recreate the same functionality in our project while following our existing UI, color palette, typography, spacing, components, coding style, and folder structure.
- First check existing CSS, JS, libraries, CDNs, icons, alerts, modals, animations, and reusable components already available in the project and use them wherever possible.
- Do not introduce a different design system.

**Execution Steps (Use your MCP tools):**

1. **Analyze:** Scan the attached UI screenshot. Map out all tables, form fields, buttons, and modals.
2. **Build:** Use your file-writing tools to create the new PHP file(s) in the Target Path.
3. **Integrate:** Use your file-editing tools to add the new page link/tab to the required sidebar file.
4. **Comply:** Read and strictly follow all architectural, UI, CSS, and JS rules defined in `schoolerp/prompts/CLAUDE.md`.

**Deliverable:**
Execute the file creation and updates via your tools immediately. Do not ask for permission to proceed. Output only a concise summary of the files you successfully created/modified and any `// TODO` action items.
