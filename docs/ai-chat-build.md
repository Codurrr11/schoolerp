**Task:** Create new Page for the tab in sidebar -
Edit Profile:
: **[edit profile]**

we need to r

New Folder will be created Here if (first check already created folder):
**Target Path:** `schoolerp/modules/school/`

## For Sql my sqltool is connected you can directly update inside the database according to the need

Create if there is new folder is required then first create folder then create page inside and then also link the sidebar path also for the active new page or folder .

Work inside the school folder and school ID should be proper integrated there in db table.


**References (Read for syntax, layout, & standards):**

- `/modules/school/` (Analyze any PHP Folder here if required create new then do)
- `assets/css/main.css`
- `assets/css/responsive.css`
- `assets/js/app.js`


## Task IF creating new page

- Scan the attached image and identify all UI elements, tables, forms, buttons, filters, actions, fields, modals, tabs, and features.
- Recreate the same functionality in our project while following our existing UI, color palette, typography, spacing, components, coding style, and folder structure.
- First check existing CSS, JS, libraries, CDNs, icons, alerts, modals, animations, and reusable components already available in the project and use them wherever possible.
- Do not introduce a different design system.

**Execution Rules:**

- **Do not stop:** If logic is unclear, make a logical assumption, add a `// TODO: [Explanation]` comment, and continue building.
- **UI & Code:** Strictly match existing project UI, components, colors, and code standards. Do not redesign or refactor unrelated code.
- **CSS:** NO inline CSS or `<style>` tags. Use existing classes. If essential, safely append new CSS to `main.css`.
- **JS:** NO inline JS. Reuse existing alerts, modals, and validations. If essential, safely append new JS to `app.js`.
